{% extends 'base.html.twig' %}

{% block title %}Modifier Bien{% endblock %}

{% block body %}

	<div class="container mt-3">

	    <h1>Modifier un bien immobilier</h1>

		{{ include('bien/_form.html.twig', {'button_label': 'Modifier'}) }}

		{{ include('bien/_delete_form.html.twig') }}

	    <div class="row text-center">
            <div class="col-12">
                
               <a class="btn btn-md btn-info" style="border-radius: 10px" href="{{ path('bien_index') }}">Retour à la liste</a>
               
            </div>
        </div>

	</div>

{% endblock %}
