{{ form_start(form) }}
    {{ form_row(form.titre) }}
    {{ form_row(form.categorie) }}
    {{ form_row(form.type) }}
    {{ form_row(form.description) }}
    {{ form_row(form.prix) }}
    {{ form_row(form.photoFile) }}
   	<button class="btn btn-md btn-primary" style="border-radius: 10px">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}